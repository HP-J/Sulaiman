cache:
  paths:
  - node_modules/

test:
  script:
    - echo "export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true && npm install" | sh
    - npm test
  only:
    - development
  tags:
    - arch
    - linux
    - 64bit

build:
  image: node:slim
  script:
    - echo "hello world"
  only:
    - development
  tags:
    - docker